# Pinned because the build depends on Shapely 2.x semantics specifically:
# shapely.contains_xy, shapely.validation.make_valid, and STRtree.query
# returning INDICES (Shapely 1.x returns geometries, which would silently
# corrupt every spatial join in build_app_data.py).
geopandas==1.1.4
shapely==2.1.2
pyproj==3.7.1
pandas==2.3.3
numpy==2.2.6
pyogrio==0.13.0
PyYAML==6.0.3
matplotlib==3.10.9        # only scripts/render_slice.py needs this
